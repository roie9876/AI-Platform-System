---
phase: 31-auth-gateway-native-ui-access
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/base/auth-gateway/deployment.yaml
  - k8s/base/auth-gateway/service.yaml
  - k8s/base/auth-gateway/hpa.yaml
  - k8s/base/auth-gateway/pdb.yaml
  - k8s/base/auth-gateway/ingress-agents.yaml
  - k8s/cert-manager/wildcard-certificate.yaml
  - k8s/overlays/tenant-template/network-policy.yaml
  - hooks/postprovision.sh
autonomous: true
requirements:
  - NATIVEUI-01
  - NATIVEUI-02

must_haves:
  truths:
    - "Auth gateway K8s manifests exist and pass dry-run validation"
    - "Wildcard Ingress routes *.agents.{domain} to auth-gateway service"
    - "Auth gateway image is built and pushed to ACR by postprovision.sh"
    - "Auth gateway K8s resources are only deployed when AGENTS_DOMAIN is set"
    - "Tenant NetworkPolicy allows auth-gateway ingress on port 18789/18790"
    - "Wildcard certificate is created in aiplatform namespace for Ingress"
  artifacts:
    - path: "k8s/base/auth-gateway/deployment.yaml"
      provides: "Auth gateway Deployment spec"
      contains: "auth-gateway"
    - path: "k8s/base/auth-gateway/service.yaml"
      provides: "Auth gateway ClusterIP Service"
      contains: "port: 8000"
    - path: "k8s/base/auth-gateway/ingress-agents.yaml"
      provides: "Wildcard Ingress template"
      contains: "AGENTS_DOMAIN"
    - path: "hooks/postprovision.sh"
      provides: "Conditional auth-gateway deployment"
      contains: "auth-gateway"
  key_links:
    - from: "k8s/base/auth-gateway/ingress-agents.yaml"
      to: "k8s/base/auth-gateway/service.yaml"
      via: "backend.service.name: auth-gateway"
      pattern: "auth-gateway"
    - from: "hooks/postprovision.sh"
      to: "k8s/base/auth-gateway/"
      via: "conditional kubectl apply"
      pattern: "AGENTS_DOMAIN.*auth-gateway"
---

<objective>
Create K8s infrastructure for the auth gateway: Deployment, Service, HPA, PDB, wildcard Ingress template, conditional deployment in postprovision.sh, and NetworkPolicy updates.

Purpose: Without K8s infrastructure, the auth gateway service can't run in AKS. The conditional deployment ensures `azd up` works without a domain configured.
Output: All K8s manifests and deployment automation ready.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-auth-gateway-native-ui-access/31-CONTEXT.md
@.planning/phases/31-auth-gateway-native-ui-access/31-RESEARCH.md

<interfaces>
<!-- K8s manifest patterns from token-proxy (copy and adapt) -->

From k8s/base/token-proxy/deployment.yaml:
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: token-proxy
  labels:
    app: token-proxy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: token-proxy
  template:
    metadata:
      labels:
        app: token-proxy
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: aiplatform-workload
      containers:
        - name: token-proxy
          image: ${ACR_SERVER}/aiplatform-token-proxy:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: aiplatform-config
          env: [COSMOS_ENDPOINT, AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_WORKLOAD_CLIENT_ID, APPLICATIONINSIGHTS_CONNECTION_STRING, AZURE_OPENAI_BASE from secrets]
          volumeMounts: [{name: secrets-store, mountPath: /mnt/secrets-store}]
          livenessProbe: {httpGet /healthz :8080, initialDelay 5, period 10}
          readinessProbe: {httpGet /readyz :8080, initialDelay 10, period 5}
          startupProbe: {httpGet /startupz :8080, initialDelay 3, failureThreshold 30, period 2}
          resources: {requests: 100m/256Mi, limits: 500m/512Mi}
      volumes: [{name: secrets-store, csi: secrets-store.csi.k8s.io, secretProviderClass: aiplatform-keyvault}]
```

From k8s/base/token-proxy/service.yaml:
```yaml
apiVersion: v1
kind: Service
metadata:
  name: token-proxy
spec:
  type: ClusterIP
  ports: [{port: 8080, targetPort: 8080}]
  selector: {app: token-proxy}
```

From k8s/base/ingress.yaml (AGC annotations):
```yaml
annotations:
  alb.networking.azure.io/alb-id: "${AGC_RESOURCE_ID}"
  alb.networking.azure.io/ssl-redirect: "true"
spec:
  ingressClassName: azure-alb-external
```

From k8s/overlays/tenant-template/network-policy.yaml:
```yaml
ingress:
  - from:
      - namespaceSelector:
          matchLabels:
            app.kubernetes.io/name: alb-controller
  - from:
      - podSelector: {}
```

From hooks/postprovision.sh (cert-manager conditional block ~line 228):
```bash
if [ -n "$AGENTS_DOMAIN" ]; then
  step "Step 8: Cert-Manager (Custom Domain)"
  ...
  for f in k8s/cert-manager/*.yaml; do
    envsubst < "$f" | kubectl apply -f -
  done
fi
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth gateway K8s manifests</name>
  <files>
    k8s/base/auth-gateway/deployment.yaml
    k8s/base/auth-gateway/service.yaml
    k8s/base/auth-gateway/hpa.yaml
    k8s/base/auth-gateway/pdb.yaml
    k8s/base/auth-gateway/ingress-agents.yaml
  </files>
  <read_first>
    k8s/base/token-proxy/deployment.yaml
    k8s/base/token-proxy/service.yaml
    k8s/base/token-proxy/hpa.yaml
    k8s/base/token-proxy/pdb.yaml
    k8s/base/ingress.yaml
    k8s/base/kustomization.yaml
  </read_first>
  <action>
Create `k8s/base/auth-gateway/` directory with these files:

**deployment.yaml** — Copy from token-proxy/deployment.yaml, adapt:
- `name: auth-gateway` (all labels, selector, metadata)
- `image: ${ACR_SERVER}/aiplatform-auth-gateway:latest`
- `containerPort: 8000` (not 8080 like token-proxy)
- Same `serviceAccountName: aiplatform-workload`
- Same `azure.workload.identity/use: "true"` label
- Same `envFrom: configMapRef: aiplatform-config`
- `env` section — keep: `COSMOS_ENDPOINT`, `AZURE_CLIENT_ID`, `AZURE_TENANT_ID`, `AZURE_WORKLOAD_CLIENT_ID`, `APPLICATIONINSIGHTS_CONNECTION_STRING` from secretKeyRef `aiplatform-secrets`. Remove `AZURE_OPENAI_BASE` (not needed). Add:
  - `ENTRA_APP_CLIENT_ID` from secretKeyRef `aiplatform-secrets` key `ENTRA_APP_CLIENT_ID`
  - `ENTRA_CLIENT_SECRET` from secretKeyRef `aiplatform-secrets` key `ENTRA_CLIENT_SECRET`
  - `AGENTS_DOMAIN` from configMapKeyRef `aiplatform-config` key `AGENTS_DOMAIN`
  - `SERVICE_NAME` value `"auth-gateway"`
- Health probes: same pattern but on port `8000`:
  - livenessProbe: httpGet /healthz :8000, initialDelaySeconds 5, periodSeconds 10
  - readinessProbe: httpGet /readyz :8000, initialDelaySeconds 10, periodSeconds 5
  - startupProbe: httpGet /startupz :8000, initialDelaySeconds 3, failureThreshold 30, periodSeconds 2
- Resources: requests 100m/256Mi, limits 500m/512Mi (same as token-proxy)
- Same secrets-store volume mount

**service.yaml** — ClusterIP service on port 8000:
```yaml
apiVersion: v1
kind: Service
metadata:
  name: auth-gateway
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
  selector:
    app: auth-gateway
```

**hpa.yaml** — HPA 2-5 replicas, 60% CPU (same as token-proxy):
```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: auth-gateway
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: auth-gateway
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
```

**pdb.yaml** — PDB minAvailable 1 (same as token-proxy):
```yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: auth-gateway
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: auth-gateway
```

**ingress-agents.yaml** — Wildcard Ingress template (variables substituted by postprovision.sh):
```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: aiplatform-agents-ingress
  annotations:
    alb.networking.azure.io/alb-id: "${AGC_RESOURCE_ID}"
    alb.networking.azure.io/ssl-redirect: "true"
spec:
  ingressClassName: azure-alb-external
  tls:
    - hosts:
        - "*.agents.${AGENTS_DOMAIN}"
        - "agents.${AGENTS_DOMAIN}"
      secretName: wildcard-agents-tls
  rules:
    - host: "*.agents.${AGENTS_DOMAIN}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: auth-gateway
                port:
                  number: 8000
    - host: "agents.${AGENTS_DOMAIN}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: auth-gateway
                port:
                  number: 8000
```

Per D-14: Two separate Ingress resources (platform hostless + agents wildcard). Per D-17: auth-gateway is sole backend for agents Ingress. Per D-20: single wildcard, no per-agent Ingress.

NOTE: Do NOT add auth-gateway manifests to `k8s/base/kustomization.yaml`. Per D-01, these resources are conditionally deployed by postprovision.sh only when AGENTS_DOMAIN is set.
  </action>
  <acceptance_criteria>
    - `k8s/base/auth-gateway/deployment.yaml` exists and contains `name: auth-gateway`
    - `k8s/base/auth-gateway/deployment.yaml` contains `containerPort: 8000`
    - `k8s/base/auth-gateway/deployment.yaml` contains `ENTRA_CLIENT_SECRET`
    - `k8s/base/auth-gateway/deployment.yaml` contains `AGENTS_DOMAIN`
    - `k8s/base/auth-gateway/deployment.yaml` contains `serviceAccountName: aiplatform-workload`
    - `k8s/base/auth-gateway/service.yaml` contains `port: 8000`
    - `k8s/base/auth-gateway/hpa.yaml` contains `minReplicas: 2`
    - `k8s/base/auth-gateway/pdb.yaml` contains `minAvailable: 1`
    - `k8s/base/auth-gateway/ingress-agents.yaml` contains `*.agents.${AGENTS_DOMAIN}`
    - `k8s/base/auth-gateway/ingress-agents.yaml` contains `auth-gateway`
    - `k8s/base/auth-gateway/ingress-agents.yaml` contains `wildcard-agents-tls`
    - `k8s/base/kustomization.yaml` does NOT contain `auth-gateway` (conditional deploy)
  </acceptance_criteria>
  <verify>
    <automated>test -f k8s/base/auth-gateway/deployment.yaml && test -f k8s/base/auth-gateway/service.yaml && test -f k8s/base/auth-gateway/hpa.yaml && test -f k8s/base/auth-gateway/pdb.yaml && test -f k8s/base/auth-gateway/ingress-agents.yaml && grep -q "auth-gateway" k8s/base/auth-gateway/deployment.yaml && grep -q "AGENTS_DOMAIN" k8s/base/auth-gateway/ingress-agents.yaml && ! grep -q "auth-gateway" k8s/base/kustomization.yaml && echo "PASS"</automated>
  </verify>
  <done>All 5 auth-gateway K8s manifests created, following token-proxy patterns, NOT added to kustomization.yaml (conditional deployment).</done>
</task>

<task type="auto">
  <name>Task 2: Update postprovision.sh, wildcard certificate, and network policy</name>
  <files>
    hooks/postprovision.sh
    k8s/cert-manager/wildcard-certificate.yaml
    k8s/overlays/tenant-template/network-policy.yaml
  </files>
  <read_first>
    hooks/postprovision.sh
    k8s/cert-manager/wildcard-certificate.yaml
    k8s/cert-manager/clusterissuer.yaml
    k8s/overlays/tenant-template/network-policy.yaml
  </read_first>
  <action>
**1. Update `hooks/postprovision.sh`** — Three changes:

**a) Add auth-gateway Docker image build (after MCP server images, before Frontend):**
Insert after the MCP server build loop (after `done` following `echo -e "  ${GREEN}✓ aiplatform-mcp-${MCP_SVC}${NC}"`):

```bash
# Auth gateway (build context: backend/)
echo -e "  ${BLUE}Building aiplatform-auth-gateway...${NC}"
az acr build \
  --registry "${ACR_NAME}" \
  --image "aiplatform-auth-gateway:${GIT_SHA}" \
  --image "aiplatform-auth-gateway:latest" \
  --file "backend/microservices/auth_gateway/Dockerfile" \
  --platform linux/amd64 \
  backend/
echo -e "  ${GREEN}✓ aiplatform-auth-gateway${NC}"
```

Update the total image count comment from `11 images` to `12 images`.

**b) Add AGENTS_DOMAIN to configmap vars** — In step 5 (Prepare K8s Manifests), after CORS_ORIGINS substitution, add substitution of `AGENTS_DOMAIN` in configmap (if we need it). Actually, AGENTS_DOMAIN is already read from azd env vars at the top. We need to add it to the configmap YAML or ensure the configmap has it. For now, the auth-gateway deployment reads it from configMapRef, so add this substitution to configmap.yaml (add `AGENTS_DOMAIN: "${AGENTS_DOMAIN}"` to configmap).

Actually, simpler approach: the deployment.yaml references `configMapKeyRef: aiplatform-config` key `AGENTS_DOMAIN`. The configmap.yaml needs this key. Add a `sed` substitution:
```bash
sed -i.bak "s|\${AGENTS_DOMAIN}|${AGENTS_DOMAIN:-}|g" "$TEMP_DIR/base/configmap.yaml"
```
after the existing `sed` commands for configmap in step 5.

**c) Add conditional auth-gateway deployment (inside the existing AGENTS_DOMAIN block, after cert-manager, before the summary):**

Expand the existing `if [ -n "$AGENTS_DOMAIN" ]; then` block at step 8 to also deploy auth-gateway resources:

After the `kubectl wait` for wildcard certificate, add:

```bash
  # Deploy auth-gateway manifests
  step "Step 8.1: Auth Gateway (Custom Domain)"
  
  TEMP_AUTH_DIR="$TEMP_DIR/auth-gateway"
  mkdir -p "$TEMP_AUTH_DIR"
  cp k8s/base/auth-gateway/*.yaml "$TEMP_AUTH_DIR/"
  
  # Substitute variables in auth-gateway manifests
  find "$TEMP_AUTH_DIR" -name "*.yaml" -exec sed -i.bak \
    -e "s|\${ACR_SERVER}|${ACR_SERVER}|g" \
    -e "s|\${AGENTS_DOMAIN}|${AGENTS_DOMAIN}|g" \
    -e "s|\${AGC_RESOURCE_ID}|${AGC_RESOURCE_ID}|g" {} \;
  find "$TEMP_AUTH_DIR" -name "*.bak" -delete
  
  kubectl apply -f "$TEMP_AUTH_DIR/" --namespace aiplatform
  echo -e "  ${GREEN}✓ Auth gateway deployed${NC}"
  
  # Add auth-gateway to rollout wait list
  echo -n "  Waiting for auth-gateway... "
  if kubectl rollout status "deployment/auth-gateway" -n aiplatform --timeout=300s 2>/dev/null; then
    echo -e "${GREEN}ready${NC}"
  else
    echo -e "${YELLOW}timeout (may need secrets configured)${NC}"
  fi
```

Also update the `ALL_DEPLOYMENTS` array in step 7 to NOT include auth-gateway (since it's conditional). The rollout wait for auth-gateway is handled in the conditional block above.

**2. Update `k8s/cert-manager/wildcard-certificate.yaml`:**

Change the namespace from `cert-manager` to `aiplatform` so the TLS Secret is created in the same namespace as the Ingress:

```yaml
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-agents-tls
  namespace: aiplatform
spec:
  secretName: wildcard-agents-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.agents.${AGENTS_DOMAIN}"
    - "agents.${AGENTS_DOMAIN}"
```

Key changes from current:
- `namespace: aiplatform` (was `cert-manager`)  
- `dnsNames` now include `agents.` prefix: `*.agents.${AGENTS_DOMAIN}` and `agents.${AGENTS_DOMAIN}` (was `*.${AGENTS_DOMAIN}` and `${AGENTS_DOMAIN}`)

**3. Update `k8s/overlays/tenant-template/network-policy.yaml`:**

Add a new ingress rule to allow auth-gateway pods from `aiplatform` namespace to reach OpenClaw pods on ports 18789 and 18790. Add this rule AFTER the existing ALB Controller rule and BEFORE the same-namespace rule:

```yaml
    # Allow from auth-gateway in aiplatform namespace (native UI proxy)
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: aiplatform
          podSelector:
            matchLabels:
              app: auth-gateway
      ports:
        - protocol: TCP
          port: 18789
        - protocol: TCP
          port: 18790
```

Also add egress rule for auth-gateway to reach tenant pods. Actually, the tenant network policy applies to TENANT namespace pods (not aiplatform). The auth-gateway is in aiplatform namespace and its egress is not restricted by the tenant NetworkPolicy. But the tenant NetworkPolicy INGRESS rules block incoming connections from aiplatform namespace. So the ingress rule above is what's needed.
  </action>
  <acceptance_criteria>
    - `hooks/postprovision.sh` contains `aiplatform-auth-gateway`
    - `hooks/postprovision.sh` contains `auth_gateway/Dockerfile`
    - `hooks/postprovision.sh` contains `Step 8.1: Auth Gateway`
    - `hooks/postprovision.sh` contains `kubectl apply -f "$TEMP_AUTH_DIR/"` inside AGENTS_DOMAIN block
    - `k8s/cert-manager/wildcard-certificate.yaml` contains `namespace: aiplatform`
    - `k8s/cert-manager/wildcard-certificate.yaml` contains `*.agents.${AGENTS_DOMAIN}`
    - `k8s/overlays/tenant-template/network-policy.yaml` contains `app: auth-gateway`
    - `k8s/overlays/tenant-template/network-policy.yaml` contains `port: 18789`
    - `bash -n hooks/postprovision.sh` exits 0 (valid shell syntax)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "aiplatform-auth-gateway" hooks/postprovision.sh && grep -q "Step 8.1" hooks/postprovision.sh && grep -q "namespace: aiplatform" k8s/cert-manager/wildcard-certificate.yaml && grep -q "auth-gateway" k8s/overlays/tenant-template/network-policy.yaml && bash -n hooks/postprovision.sh && echo "PASS"</automated>
  </verify>
  <done>postprovision.sh builds auth-gateway image, conditionally deploys K8s resources when AGENTS_DOMAIN set. Wildcard certificate targets aiplatform namespace. Tenant NetworkPolicy allows auth-gateway ingress.</done>
</task>

</tasks>

<verification>
- All K8s manifests exist in k8s/base/auth-gateway/
- postprovision.sh has valid bash syntax
- Auth-gateway deployment is conditional on AGENTS_DOMAIN
- NetworkPolicy allows cross-namespace auth-gateway access
- Wildcard certificate Secret in correct namespace (aiplatform)
</verification>

<success_criteria>
- K8s manifests ready for conditional deployment
- postprovision.sh builds image and deploys when AGENTS_DOMAIN is set
- No changes to kustomization.yaml (auth-gateway is not part of base deployment)
- Tenant NetworkPolicy allows auth-gateway to proxy to OpenClaw pods
</success_criteria>

<output>
After completion, create `.planning/phases/31-auth-gateway-native-ui-access/31-02-SUMMARY.md`
</output>

---
phase: 26-tenant-context-wiring
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - frontend/src/app/dashboard/observability/page.tsx
  - frontend/src/app/dashboard/observability/tokens/page.tsx
  - frontend/src/app/dashboard/observability/logs/page.tsx
  - frontend/src/app/dashboard/observability/costs/page.tsx
  - frontend/src/app/dashboard/mcp-tools/page.tsx
  - frontend/src/app/dashboard/mcp-tools/servers/page.tsx
autonomous: false
requirements:
  - UI-02

must_haves:
  truths:
    - "Switching tenants causes observability dashboard, tokens, logs, and costs pages to refetch data"
    - "Switching tenants causes MCP tools and MCP servers pages to refetch data"
    - "No dashboard page shows data from a previously selected tenant after switching"
  artifacts:
    - path: "frontend/src/app/dashboard/observability/page.tsx"
      provides: "Tenant-scoped observability dashboard"
      contains: "selectedTenantId"
    - path: "frontend/src/app/dashboard/mcp-tools/page.tsx"
      provides: "Tenant-scoped MCP tools listing"
      contains: "selectedTenantId"
  key_links:
    - from: "frontend/src/app/dashboard/observability/page.tsx"
      to: "frontend/src/contexts/tenant-context.tsx"
      via: "useTenant() hook"
      pattern: "useTenant\\(\\)"
    - from: "frontend/src/app/dashboard/mcp-tools/page.tsx"
      to: "frontend/src/contexts/tenant-context.tsx"
      via: "useTenant() hook"
      pattern: "useTenant\\(\\)"
---

<objective>
Wire useTenant() into all remaining data-fetching dashboard pages (observability suite + MCP tools) and verify the complete tenant context wiring.

Purpose: Completes UI-02 — every dashboard page that fetches data filters by selected tenant.
Output: 6 remaining pages wired; human verifies that tenant switching works end-to-end across all pages.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-tenant-context-wiring/26-01-SUMMARY.md

@frontend/src/contexts/tenant-context.tsx
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useTenant() into observability and MCP tools pages</name>
  <files>
    frontend/src/app/dashboard/observability/page.tsx,
    frontend/src/app/dashboard/observability/tokens/page.tsx,
    frontend/src/app/dashboard/observability/logs/page.tsx,
    frontend/src/app/dashboard/observability/costs/page.tsx,
    frontend/src/app/dashboard/mcp-tools/page.tsx,
    frontend/src/app/dashboard/mcp-tools/servers/page.tsx
  </files>
  <read_first>
    - frontend/src/app/dashboard/observability/page.tsx
    - frontend/src/app/dashboard/observability/tokens/page.tsx
    - frontend/src/app/dashboard/observability/logs/page.tsx
    - frontend/src/app/dashboard/observability/costs/page.tsx
    - frontend/src/app/dashboard/mcp-tools/page.tsx
    - frontend/src/app/dashboard/mcp-tools/servers/page.tsx
    - frontend/src/contexts/tenant-context.tsx
    - .planning/phases/26-tenant-context-wiring/26-01-SUMMARY.md
  </read_first>
  <action>
Apply the same pattern established in Plan 01 to all 6 remaining pages:

1. Add import: `import { useTenant } from "@/contexts/tenant-context";`
2. Add hook call at start of component: `const { selectedTenantId } = useTenant();`
3. Add `selectedTenantId` to the useCallback/useEffect dependency array that triggers data fetching

**Page-specific details:**

**observability/page.tsx (ObservabilityPage):**
- Uses `fetchData` useCallback with deps `[timeRange, granularity]`
- Add `selectedTenantId` to fetchData deps: `[timeRange, granularity, selectedTenantId]`
- useEffect depends on `[fetchData]` — will re-trigger automatically

**observability/tokens/page.tsx (TokensPage):**
- Uses `fetchData` useCallback with deps `[timeRange, granularity]`
- Add `selectedTenantId` to fetchData deps: `[timeRange, granularity, selectedTenantId]`

**observability/logs/page.tsx (LogsPage):**
- Uses `fetchData` useCallback with deps `[timeRange, offset]`
- Add `selectedTenantId` to fetchData deps: `[timeRange, offset, selectedTenantId]`

**observability/costs/page.tsx (CostsPage):**
- Uses `fetchData` useCallback with deps `[timeRange]`
- Add `selectedTenantId` to fetchData deps: `[timeRange, selectedTenantId]`

**mcp-tools/page.tsx (MCPToolsPage):**
- Uses `fetchData` useCallback with deps `[]`
- Add `selectedTenantId` to fetchData deps: `[selectedTenantId]`

**mcp-tools/servers/page.tsx (MCPServersPage):**
- Uses `fetchServers` useCallback
- Add `selectedTenantId` to fetchServers deps array
  </action>
  <verify>
    <automated>cd frontend && for f in src/app/dashboard/observability/page.tsx src/app/dashboard/observability/tokens/page.tsx src/app/dashboard/observability/logs/page.tsx src/app/dashboard/observability/costs/page.tsx src/app/dashboard/mcp-tools/page.tsx src/app/dashboard/mcp-tools/servers/page.tsx; do echo "=== $f ===" && grep -c "useTenant" "$f" && grep -c "selectedTenantId" "$f"; done && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 pages contains `import { useTenant } from "@/contexts/tenant-context";`
    - Each of the 6 pages contains `const { selectedTenantId } = useTenant();`
    - Each of the 6 pages has `selectedTenantId` in a useCallback or useEffect dependency array
    - `npx tsc --noEmit` reports no type errors in modified files
  </acceptance_criteria>
  <done>All 6 remaining pages refetch when tenant changes. Combined with Plan 01, all 12 data-fetching dashboard pages are tenant-aware.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify tenant switching updates all pages</name>
  <files>None — verification only</files>
  <action>
Human verifies complete tenant context wiring across all 12 dashboard pages:
- apiFetch auto-injects X-Tenant-Id header
- TenantContext syncs to API module
- 12 pages refetch data when tenant selector changes:
  agents, tools, data-sources, models, workflows, evaluations,
  observability (main + tokens + logs + costs), mcp-tools (main + servers)

Steps to verify:
1. Start the frontend: `cd frontend && npm run dev`
2. Log in as a platform admin (who can see the tenant selector)
3. Open browser DevTools → Network tab
4. Navigate to /dashboard/agents — verify API request includes `X-Tenant-Id` header
5. Switch to a different tenant in the TenantSelector dropdown
6. Verify the agents page refetches — new request appears in Network tab with updated X-Tenant-Id
7. Navigate to /dashboard/tools — verify refetch with correct X-Tenant-Id
8. Navigate to /dashboard/observability — verify refetch with correct X-Tenant-Id
9. Navigate to /dashboard/mcp-tools — verify refetch with correct X-Tenant-Id
10. Switch tenant again — verify all previously visited pages would refetch on revisit
  </action>
  <verify>Human confirms: Type "approved" or describe issues</verify>
  <done>All 12 dashboard pages confirmed to filter by selected tenant with no cross-tenant data leakage</done>
</task>

</tasks>

<verification>
1. `grep -rn "useTenant" frontend/src/app/dashboard/` shows useTenant in all 12 target pages
2. `grep -rn "selectedTenantId" frontend/src/app/dashboard/` shows dependency wiring in all 12 pages
3. `cd frontend && npx tsc --noEmit` passes with no errors
4. Human verified tenant switching updates data across all pages
</verification>

<success_criteria>
- All 12 data-fetching dashboard pages import and use useTenant()
- All 12 pages refetch data when selectedTenantId changes
- X-Tenant-Id header visible in browser DevTools on every API request
- No cross-tenant data leakage when switching tenants
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/26-tenant-context-wiring/26-02-SUMMARY.md`
</output>

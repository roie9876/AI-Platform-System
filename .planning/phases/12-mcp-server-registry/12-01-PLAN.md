---
phase: 12-mcp-server-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/mcp_server.py
  - backend/alembic/versions/011_mcp_server_registry.py 
  - backend/app/api/v1/schemas.py
  - backend/app/api/v1/mcp_servers.py
  - backend/app/api/v1/router.py
  - backend/tests/test_mcp_server_registry.py
autonomous: true
requirements: [MCP-04, MCP-05, MCP-06]

must_haves:
  truths:
    - "User can register a new MCP server with URL, auth type, and metadata"
    - "User can list all registered MCP servers for their tenant"
    - "User can update an existing MCP server registration"
    - "User can delete an MCP server registration"
    - "User can view connection status of registered MCP servers"
  artifacts:
    - path: "backend/app/models/mcp_server.py"
      provides: "SQLAlchemy model for MCP server registrations"
    - path: "backend/app/api/v1/mcp_servers.py"
      provides: "CRUD API endpoints for MCP servers"
    - path: "backend/alembic/versions/011_mcp_server_registry.py"
      provides: "Database migration for mcp_servers table"
  key_links:
    - from: "backend/app/api/v1/mcp_servers.py"
      to: "backend/app/models/mcp_server.py"
      via: "SQLAlchemy queries"
    - from: "backend/app/api/v1/router.py"
      to: "backend/app/api/v1/mcp_servers.py"
      via: "include_router"
---

<objective>
Build the MCP Server Registry — database model, migration, and full CRUD API for managing MCP server connections.

Purpose: Users need to register and manage their fleet of remote MCP servers before tools can be discovered (Phase 13) or invoked (Phase 14).

Output: MCPServer model, migration, CRUD endpoints, schemas, and tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md  
@.planning/phases/11-mcp-client-library/11-01-SUMMARY.md
@backend/app/models/base.py
@backend/app/models/agent.py
@backend/app/api/v1/agents.py
@backend/app/api/v1/schemas.py
@backend/app/api/v1/router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCPServer Model + Migration + Schemas</name>
  <files>backend/app/models/mcp_server.py, backend/alembic/versions/011_mcp_server_registry.py, backend/app/api/v1/schemas.py</files>
  <action>
Create MCPServer model, migration, and Pydantic request/response schemas following existing codebase patterns.
  </action>
  <verify>python -c "from app.models.mcp_server import MCPServer; print('Model OK')"</verify>
  <done>MCPServer model, migration, and schemas exist and are importable</done>
</task>

<task type="auto">
  <name>Task 2: CRUD API + Router + Tests</name>
  <files>backend/app/api/v1/mcp_servers.py, backend/app/api/v1/router.py, backend/tests/test_mcp_server_registry.py</files>
  <action>
Create CRUD endpoints and unit tests for MCP server management.
  </action>
  <verify>cd backend && python -m pytest tests/test_mcp_server_registry.py -v</verify>
  <done>All CRUD endpoints exist and tests pass</done>
</task>

</tasks>

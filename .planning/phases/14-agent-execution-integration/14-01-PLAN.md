---
phase: 14-agent-execution-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/agent_mcp_tool.py
  - backend/alembic/versions/013_agent_mcp_tools.py
  - backend/app/services/agent_execution.py
  - backend/tests/test_agent_mcp_integration.py
autonomous: true
requirements: [MCP-10, MCP-11, MCP-12]
must_haves:
  truths:
    - "Agent execution loop can invoke MCP tools alongside platform adapters and sandbox tools"
    - "MCP tool calls follow the same observability pipeline as other tool calls"
    - "MCP tools prefixed with mcp__ to avoid name collisions"
  artifacts:
    - path: "backend/app/models/agent_mcp_tool.py"
      provides: "Link table connecting agents to MCP discovered tools"
    - path: "backend/alembic/versions/013_agent_mcp_tools.py"
      provides: "Migration for agent_mcp_tools table"
    - path: "backend/app/services/agent_execution.py"
      provides: "Modified agent execution with MCP as third tool execution path"
    - path: "backend/tests/test_agent_mcp_integration.py"
      provides: "Unit tests for MCP integration in agent execution"
  key_links:
    - from: "backend/app/services/agent_execution.py"
      to: "backend/app/services/mcp_client.py"
      via: "MCPClient for tool execution"
    - from: "backend/app/services/agent_execution.py"
      to: "backend/app/models/agent_mcp_tool.py"
      via: "SQLAlchemy query for agent's MCP tools"
---

<objective>
Wire MCP tools/call into the existing agent tool-calling loop alongside platform adapters and sandbox tools, making MCP a third execution path.

Purpose: Enable agents to invoke any MCP tool attached to them during execution
Output: Modified agent_execution.py with MCP support, AgentMCPTool link model, migration, tests
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app/services/agent_execution.py
@backend/app/services/mcp_client.py
@backend/app/models/mcp_discovered_tool.py
@backend/app/models/mcp_server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: AgentMCPTool model and migration</name>
  <files>backend/app/models/agent_mcp_tool.py, backend/alembic/versions/013_agent_mcp_tools.py</files>
  <action>Create AgentMCPTool SQLAlchemy model as a link table between agents and mcp_discovered_tools. Add migration 013.</action>
  <verify>python -c "from app.models.agent_mcp_tool import AgentMCPTool; print('OK')"</verify>
  <done>Model importable, migration exists</done>
</task>

<task type="auto">
  <name>Task 2: Wire MCP into agent execution loop</name>
  <files>backend/app/services/agent_execution.py</files>
  <action>Add _load_agent_mcp_tools(), _build_mcp_tool_schemas(), _execute_mcp_tool() methods. Modify tool loading to merge MCP tool schemas (prefixed mcp__). Add MCP as third execution path in tool-calling loop.</action>
  <verify>python -m pytest tests/test_agent_mcp_integration.py -v --tb=short</verify>
  <done>10/10 tests pass covering schema building, MCP execution (success/failure/not-found/error), tool loading, model validation</done>
</task>

</tasks>

<verification>
cd backend && python -m pytest tests/test_agent_mcp_integration.py -v --tb=short
</verification>

<success_criteria>
- 10/10 tests pass
- MCP tools loaded and prefixed with mcp__ in agent execution
- MCP tool calls routed through MCPClient
- AgentMCPTool model and migration exist
</success_criteria>

<output>
After completion, create `.planning/phases/14-agent-execution-integration/14-01-SUMMARY.md`
</output>

# Phase 29: Token Proxy - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-04
**Phase:** 29-token-proxy
**Areas discussed:** Tenant & agent attribution, Budget enforcement strategy, API compatibility scope

---

## Tenant & Agent Attribution

| Option | Description | Selected |
|--------|-------------|----------|
| Path-based | Encode tenant/agent in the URL path. Guaranteed to work — no OpenClaw dependencies. Slightly longer baseUrl. | ✓ |
| Header-based | Use custom X-Tenant-Id/X-Agent-Id headers in the CR. Cleaner URLs but requires verifying OpenClaw passes them through. | |
| You decide | Agent's discretion based on what works reliably | |

**User's choice:** Path-based
**Notes:** No fallback identification method needed — all CRs are generated programmatically by openclaw_service.py.

### Follow-up: Fallback method

| Option | Description | Selected |
|--------|-------------|----------|
| Network-based fallback | Proxy extracts source namespace from K8s network context as secondary signal | |
| Path-only, no fallback | Path-based is sufficient — every CR is generated by openclaw_service.py with the correct tenant/agent path embedded | ✓ |
| You decide | Agent's discretion | |

**User's choice:** Path-only, no fallback

---

## Budget Enforcement Strategy

### Budget Mode

| Option | Description | Selected |
|--------|-------------|----------|
| Hard limits (block) | Proxy checks usage against budget on each request. When exceeded, returns 429 — immediate block. | |
| Soft limits (alert only) | No enforcement at proxy level. Async aggregation fires alerts when thresholds hit. Requests never blocked. | |
| Both (soft default, hard opt-in) | Soft by default, with per-tenant opt-in for hard limits. Both modes available. | ✓ |
| You decide | Agent's discretion | |

**User's choice:** Both (soft default, hard opt-in)

### Budget Admin

| Option | Description | Selected |
|--------|-------------|----------|
| Platform admin only | Only platform admin can set budgets per tenant. No tenant-facing UI needed. | |
| Tenant self-service | Tenant admins set their own budgets via the Tenant Admin UI. Requires API + UI work. | ✓ |
| You decide | Agent's discretion | |

**User's choice:** Tenant self-service

### Alert Channel

| Option | Description | Selected |
|--------|-------------|----------|
| Platform dashboard only | Log alerts to App Insights + Cosmos DB. No external notifications. | |
| Dashboard + notifications | App Insights + webhook/email. Proactive notifications. | ✓ |
| You decide | Agent's discretion | |

**User's choice:** Dashboard + notifications

---

## API Compatibility Scope

### API Scope

| Option | Description | Selected |
|--------|-------------|----------|
| OpenAI APIs + passthrough | Chat Completions + Responses API only. Wildcard passthrough for other paths. | |
| OpenAI APIs + Embeddings | Also explicitly handle Embeddings API for tracking on embedding calls. | |
| Multi-provider | Support any LLM provider with provider-specific token extraction. | ✓ |
| You decide | Agent's discretion | |

**User's choice:** Multi-provider

### Multi-provider Implementation Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Pluggable design, Azure-only now | Provider abstraction layer so adding providers later is a config change. Only Azure OpenAI in this phase. | ✓ |
| Azure + Anthropic now | Implement Azure OpenAI + Anthropic. Both commonly used. | |
| All providers now | Full multi-provider from day 1. | |

**User's choice:** Pluggable design, Azure-only now

---

## Agent's Discretion

- K8s Deployment, Service, HPA, PDB, NetworkPolicy specifics
- Cosmos DB TokenLogRepository implementation details
- App Insights telemetry integration approach
- Cost estimation logic
- Notification delivery mechanism
- Provider abstraction interface design
- openclaw_service.py changes for proxy baseUrl injection

## Deferred Ideas

- Token usage dashboard UI (dedicated analytics page)
- Dynamic model pricing configuration
- Aggregation service for daily/weekly token summaries

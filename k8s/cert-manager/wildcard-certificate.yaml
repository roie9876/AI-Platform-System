apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-agents-tls
  namespace: aiplatform
spec:
  secretName: wildcard-agents-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.agents.${AGENTS_DOMAIN}"
    - "agents.${AGENTS_DOMAIN}"

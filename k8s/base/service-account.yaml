apiVersion: v1
kind: ServiceAccount
metadata:
  name: aiplatform-workload
  annotations:
    azure.workload.identity/client-id: "${WORKLOAD_IDENTITY_CLIENT_ID}"
  labels:
    azure.workload.identity/use: "true"
    app.kubernetes.io/part-of: aiplatform

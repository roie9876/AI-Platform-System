apiVersion: v1
kind: Service
metadata:
  name: mcp-atlassian
  labels:
    app: mcp-atlassian
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
  selector:
    app: mcp-atlassian

apiVersion: v1
kind: Service
metadata:
  name: mcp-github
  labels:
    app: mcp-github
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
  selector:
    app: mcp-github

apiVersion: apps/v1
kind: Deployment
metadata:
  name: token-proxy
  labels:
    app: token-proxy
    app.kubernetes.io/name: token-proxy
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: token-proxy
  template:
    metadata:
      labels:
        app: token-proxy
        app.kubernetes.io/name: token-proxy
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: aiplatform-workload
      containers:
        - name: token-proxy
          image: ${ACR_SERVER}/aiplatform-token-proxy:latest
          ports:
            - containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: aiplatform-config
          env:
            - name: SERVICE_NAME
              value: "llm-proxy"
            - name: COSMOS_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: aiplatform-secrets
                  key: COSMOS_ENDPOINT
            - name: AZURE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: aiplatform-secrets
                  key: AZURE_CLIENT_ID
            - name: AZURE_TENANT_ID
              valueFrom:
                secretKeyRef:
                  name: aiplatform-secrets
                  key: AZURE_TENANT_ID
            - name: AZURE_WORKLOAD_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: aiplatform-secrets
                  key: AZURE_WORKLOAD_CLIENT_ID
            - name: APPLICATIONINSIGHTS_CONNECTION_STRING
              valueFrom:
                secretKeyRef:
                  name: aiplatform-secrets
                  key: APPLICATIONINSIGHTS_CONNECTION_STRING
            - name: AZURE_OPENAI_BASE
              valueFrom:
                secretKeyRef:
                  name: aiplatform-secrets
                  key: AZURE_OPENAI_ENDPOINT
          volumeMounts:
            - name: secrets-store
              mountPath: "/mnt/secrets-store"
              readOnly: true
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          startupProbe:
            httpGet:
              path: /startupz
              port: 8080
            initialDelaySeconds: 3
            failureThreshold: 30
            periodSeconds: 2
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: aiplatform-keyvault

apiVersion: v1
kind: Service
metadata:
  name: tool-executor
  labels:
    app: tool-executor
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: tool-executor

apiVersion: v1
kind: Service
metadata:
  name: mcp-proxy
  labels:
    app: mcp-proxy
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: mcp-proxy

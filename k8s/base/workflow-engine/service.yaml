apiVersion: v1
kind: Service
metadata:
  name: workflow-engine
  labels:
    app: workflow-engine
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: workflow-engine

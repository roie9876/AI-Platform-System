apiVersion: v1
kind: Service
metadata:
  name: agent-executor
  labels:
    app: agent-executor
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: agent-executor

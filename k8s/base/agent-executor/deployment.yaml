apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-executor
  labels:
    app: agent-executor
    app.kubernetes.io/name: agent-executor
    app.kubernetes.io/component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-executor
  template:
    metadata:
      labels:
        app: agent-executor
        app.kubernetes.io/name: agent-executor
    spec:
      serviceAccountName: aiplatform-workload
      containers:
        - name: agent-executor
          image: stumsftaiplatformprodacr.azurecr.io/aiplatform-agent-executor:latest
          ports:
            - containerPort: 8000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: aiplatform-config
          env:
            - name: SERVICE_NAME
              value: "agent-executor"
            - name: COSMOS_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: aiplatform-secrets
                  key: COSMOS_ENDPOINT
            - name: AZURE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: aiplatform-secrets
                  key: AZURE_CLIENT_ID
            - name: AZURE_TENANT_ID
              valueFrom:
                secretKeyRef:
                  name: aiplatform-secrets
                  key: AZURE_TENANT_ID
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
          startupProbe:
            httpGet:
              path: /startupz
              port: 8000
            initialDelaySeconds: 3
            failureThreshold: 30
            periodSeconds: 2
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi

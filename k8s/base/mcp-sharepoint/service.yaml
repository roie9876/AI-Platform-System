apiVersion: v1
kind: Service
metadata:
  name: mcp-sharepoint
  labels:
    app: mcp-sharepoint
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
  selector:
    app: mcp-sharepoint
